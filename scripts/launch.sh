#!/bin/bash
# Auto-launched by /meta-diagnostics after report and dashboard files are written.
# Never run this manually — the skill handles invocation.
cd ~/meta-diagnostics
streamlit run dashboard.py
